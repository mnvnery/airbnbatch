<%= simple_form_for (@booking) do |f| %>
        <%= f.input :start_date %>
        <%= f.input :end_date %>
        <%= f.submit "Edit", class: "btn btn-primary"%>
        <%= link_to "Back", booking_path(@booking), class: "btn btn-outline-primary m-2"%>
<% end%>

